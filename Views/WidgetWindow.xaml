<Window x:Class="FxVolatilityImport.Views.WidgetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FX Vol Widget"
        Height="155" Width="320"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        
        <!-- Spinner rotation animation -->
        <Storyboard x:Key="SpinAnimation" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetName="SpinnerRotate"
                             Storyboard.TargetProperty="Angle"
                             From="0" To="360" Duration="0:0:1"/>
        </Storyboard>
        
        <!-- Success fade out animation -->
        <Storyboard x:Key="SuccessFadeOut">
            <DoubleAnimation Storyboard.TargetName="SuccessIndicator"
                             Storyboard.TargetProperty="Opacity"
                             From="1" To="0" Duration="0:0:3" BeginTime="0:0:2"/>
        </Storyboard>
        
        <!-- Widget Button Style -->
        <Style x:Key="WidgetButton" TargetType="Button">
            <Setter Property="Background" Value="#20FFFFFF"/>
            <Setter Property="Foreground" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#30FFFFFF"/>
                                <Setter Property="Foreground" Value="#F1F5F9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#40FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Border Background="#E6101827" CornerRadius="12" BorderBrush="#334155" BorderThickness="1">
        <Grid Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📊" FontSize="14" Margin="0,0,6,0" Foreground="White"/>
                    <TextBlock Text="FX Volatility" FontSize="13" FontWeight="SemiBold" Foreground="#F1F5F9"/>
                </StackPanel>
                <Button Content="✕" HorizontalAlignment="Right" 
            Background="Transparent" Foreground="#94A3B8" 
            BorderThickness="0" FontSize="12" Cursor="Hand"
            Click="CloseButton_Click"/>
            </Grid>

            <!-- Connection Status + Currency Pairs on same row -->
            <Grid Grid.Row="1" Margin="0,8,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Connection Status -->
                <StackPanel Orientation="Horizontal">
                    <Ellipse Width="8" Height="8" Margin="0,0,8,0" VerticalAlignment="Center">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Setter Property="Fill" Value="#F87171"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                        <Setter Property="Fill" Value="#4ADE80"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock FontSize="11" Foreground="#94A3B8" VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Text" Value="Disconnected"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                        <Setter Property="Text" Value="Bloomberg Connected"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
                
                <!-- Currency Pairs count -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="Currency Pairs:" FontSize="11" Foreground="#94A3B8" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding CurrencyPairs.Count}" FontSize="11" Foreground="#F1F5F9" 
                               FontWeight="Medium" Margin="6,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
            
            <!-- Import Button -->
            <Button Grid.Row="2" 
                    Style="{StaticResource WidgetButton}"
                    Click="ImportAllButton_Click"
                    Margin="0,10,0,10"
                    IsEnabled="{Binding IsConnected}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🔄" Margin="0,0,6,0"/>
                    <TextBlock Text="Load &amp; Import All"/>
                </StackPanel>
            </Button>
            
            <!-- Status Bar with Import Indicator -->
            <Grid Grid.Row="3">
                <Border Background="#1AFFFFFF" CornerRadius="6" Padding="8,6">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Import Status Indicator -->
                        <Grid Width="18" Height="18" Margin="0,0,8,0" VerticalAlignment="Center">
                            
                            <!-- Idle state (no indicator) -->
                            <Ellipse x:Name="IdleIndicator" Width="8" Height="8" 
                                     Fill="#94A3B8" Opacity="0.3"
                                     HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Visibility" Value="Visible"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsImporting}" Value="True">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding ImportJustCompleted}" Value="True">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                            
                            <!-- Importing spinner (yellow) -->
                            <Grid x:Name="SpinnerContainer">
                                <Grid.Style>
                                    <Style TargetType="Grid">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsImporting}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>
                                <Ellipse Width="16" Height="16" 
                                         Stroke="#FBBF24" StrokeThickness="2" Opacity="0.3"/>
                                <Ellipse Width="16" Height="16" 
                                         Stroke="#FBBF24" StrokeThickness="2"
                                         StrokeDashArray="2,6"
                                         RenderTransformOrigin="0.5,0.5">
                                    <Ellipse.RenderTransform>
                                        <RotateTransform x:Name="SpinnerRotate"/>
                                    </Ellipse.RenderTransform>
                                </Ellipse>
                            </Grid>
                            
                            <!-- Success checkmark (green) -->
                            <Grid x:Name="SuccessIndicator" Opacity="0">
                                <Grid.Style>
                                    <Style TargetType="Grid">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ImportJustCompleted}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>
                                <Ellipse Width="16" Height="16" Fill="#4ADE80"/>
                                <Path Data="M4,9 L7,12 L12,6" 
                                      Stroke="#0F172A" StrokeThickness="2"
                                      HorizontalAlignment="Center" VerticalAlignment="Center"
                                      Margin="1,1,0,0"/>
                            </Grid>
                        </Grid>
                        
                        <!-- Status Text -->
                        <TextBlock Grid.Column="1"
                                   Text="{Binding StatusText}" 
                                   FontSize="10" Foreground="#CBD5E1"
                                   TextTrimming="CharacterEllipsis"
                                   VerticalAlignment="Center"/>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>